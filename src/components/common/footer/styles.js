import { css } from "lit";

export const styles = css`
  .Footer {
    width: 100%;
    background-color: var(--dark-brown);
    display: flex;
    justify-content: center;
    align-items: center;
    color: var(--primary-white);
    font-size: 18px;
    height: 60px;
  }
`;
